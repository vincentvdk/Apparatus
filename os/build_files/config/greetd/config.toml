# greetd configuration for Apparatus
# https://man.sr.ht/~kennylevinsen/greetd/

[terminal]
vt = 1

[default_session]
# Run Hyprland with greeter-specific config, which launches regreet
command = "Hyprland --config /etc/greetd/hyprland.conf"
user = "greeter"
