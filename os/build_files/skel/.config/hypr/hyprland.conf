# Apparatus Hyprland Configuration
# System defaults are sourced below. Add your customizations after the source line.
# Full config reference: /usr/share/apparatus/hypr/hyprland.conf

source = /usr/share/apparatus/hypr/hyprland.conf

# ──────────────────────────────────────────────────────────────────────────────
# User customizations below
# ──────────────────────────────────────────────────────────────────────────────

# Example: Override default terminal
# $terminal = alacritty

# Example: Add custom keybindings
# bind = $mainMod, B, exec, firefox

# Example: Adjust gaps
# general {
#     gaps_out = 20
# }
